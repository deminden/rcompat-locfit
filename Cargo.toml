[package]
name = "rcompat-locfit"
version = "0.1.3"
edition = "2021"
license = "MIT"
description = "R locfit-compatible local regression in Rust, focused on DESeq2 dispersion trend fitting."
repository = "https://github.com/deminden/rcompat-locfit"
exclude = [
    ".github/**",
    "data/**",
]

[lib]
name = "rcompat_locfit"
path = "src/lib.rs"

[dependencies]
